apiVersion: v1
kind: Service
metadata:
  name: {{ include "he-client.fullname" . }}
  labels:
    {{- include "he-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.goPort }}
      targetPort: goPort
      protocol: TCP
      name: goPort
    - port: {{ .Values.service.pythonPort }}
      targetPort: pythonPort
      protocol: TCP
      name: pythonPort
  selector:
    {{- include "he-client.selectorLabels" . | nindent 4 }}

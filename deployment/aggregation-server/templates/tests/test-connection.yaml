apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aggregation-server.fullname" . }}-test-connection"
  labels:
    {{- include "aggregation-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aggregation-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

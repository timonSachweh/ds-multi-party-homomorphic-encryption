apiVersion: v1
kind: Service
metadata:
  name: {{ include "aggregation-server.fullname" . }}
  labels:
    {{- include "aggregation-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aggregation-server.selectorLabels" . | nindent 4 }}
